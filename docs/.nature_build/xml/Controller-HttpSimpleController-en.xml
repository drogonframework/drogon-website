<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE document PUBLIC "+//IDN docutils.sourceforge.net//DTD Docutils Generic//EN//XML" "http://docutils.sourceforge.net/docs/ref/docutils.dtd">
<!-- Generated by Docutils 0.15.2 -->
<document source="C:\Users\86175\.github\drogon-all\drogon_web_community\drogon-website\docs\source\Controller-HttpSimpleController-en.rstd">
    <section ids="controller-httpsimplecontroller" names="controller\ httpsimplecontroller">
        <title>Controller HttpSimpleController</title>
        <paragraph>You could use the <emphasis>drogon_ctl</emphasis> command line tool to quickly generate custum controller class source files based on <emphasis>HttpSimpleController</emphasis>, the command format is as bellow:</paragraph>
        <literal_block highlight_args="{}" language="bash" linenos="False" xml:space="preserve">drogon_ctl create controller &lt;[namespace::]class_name&gt;</literal_block>
        <paragraph>We create one controller class named <emphasis>TestCtrl</emphasis>:</paragraph>
        <literal_block highlight_args="{}" language="bash" linenos="False" xml:space="preserve">drogon_ctl create controller TestCtrl</literal_block>
        <paragraph>As you can see, there are two new files, TestCtrl.h and TestCtrl.cc. Now, let’s have a look at them:</paragraph>
        <container classes="literal-block-wrapper" ids="id1" literal_block="True">
            <caption><strong>TestCtrl.h：</strong></caption>
            <literal_block highlight_args="{}" language="cpp" linenos="False" xml:space="preserve">#pragma once
#include &lt;drogon/HttpSimpleController.h&gt;
using namespace drogon;
class TestCtrl:public drogon::HttpSimpleController&lt;TestCtrl&gt;
{
public:
    virtual void asyncHandleHttpRequest(const HttpRequestPtr &amp;req,
                                        std::function&lt;void (const HttpResponsePtr &amp;)&gt; &amp;&amp;callback)override;
    PATH_LIST_BEGIN
    //list path definitions here;
    //PATH_ADD("/path","filter1","filter2",HttpMethod1,HttpMethod2...);
    PATH_LIST_END
};</literal_block>
        </container>
        <container classes="literal-block-wrapper" ids="id2" literal_block="True">
            <caption><strong>TestCtrl.cc</strong></caption>
            <literal_block highlight_args="{}" language="cpp" linenos="False" xml:space="preserve">#include "TestCtrl.h"
void TestCtrl::asyncHandleHttpRequest(const HttpRequestPtr &amp;req,
                                    std::function&lt;void (const HttpResponsePtr &amp;)&gt; &amp;&amp;callback)
{
    //write your application logic here
}</literal_block>
        </container>
        <paragraph>Each HttpSimpleController class can only define one Http request handler, and it is defined by a virtual function override.</paragraph>
        <paragraph>The route (or called mapping) from the URL path to the handler is done by a macro. You could add multipath mappings with the <emphasis>PATH_ADD</emphasis> macro. All <emphasis>PATH_ADD</emphasis> statements should be set between the <emphasis>PATH_LIST_BEGIN</emphasis> and <emphasis>PATH_LIST_END</emphasis> macro statements.</paragraph>
        <paragraph>The first parameter is the path to be map, parameters behind the path are constraints on this path. Currently, two types of constraints are supported. One is the <emphasis>HttpMethod</emphasis> enum Type, which means the Http method allowed. The other type is the name of the <emphasis>HttpFilter</emphasis> class. One can configure any number of these two types of constraints and there are no order requirements for them. For Filter, please refer to Filter.</paragraph>
        <paragraph>Users can register the same Simple Controller to multiple paths, or register multiple Simple Controllers on the same path (using different HTTP methods).</paragraph>
        <paragraph>You could define an HttpResponse class variable, and then use the callback() to return it:</paragraph>
        <literal_block highlight_args="{}" language="cpp" linenos="False" xml:space="preserve">//write your application logic here
auto resp=HttpResponse::newHttpResponse();
resp-&gt;setStatusCode(k200OK);
resp-&gt;setContentTypeCode(CT_TEXT_HTML);
resp-&gt;setBody("Your Page Contents");
callback(resp);</literal_block>
        <paragraph><strong>The mapping from the above path to the handler is done at compile time. In fact, the drogon framework also provides an interface for runtime completion mapping. The runtime mapping allows the user to map or modify the mapping through configuration files or other user interfaces without Recompile this program (For performance reasons, it is forbidden to add any controller mapping after running app().run() method).</strong></paragraph>
    </section>
</document>
